import styled from 'styled-components';

export const Text = styled.h3`
  font-size: 2em;
  color: #d4d4ea;
  transition: 0.2s ease;
  font-weight: 700;
  &:hover {
    color: #fff;
    transition: 0.2s ease;
  }
`;
